---
export interface Props { cols?: 1|2|3|4 }
const { cols = 2 } = Astro.props as Props;
const cls = cols === 1 ? 'md:grid-cols-1' : cols === 2 ? 'md:grid-cols-2' : cols === 3 ? 'md:grid-cols-3' : 'md:grid-cols-4';
---
<div class={`grid gap-4 ${cls}`}>
  <slot />
</div>
